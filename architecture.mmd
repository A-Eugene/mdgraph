flowchart TB
  subgraph TRUTH["TRUTH — markdown, append-only, git-versioned"]
    R1["report / topic file<br/>node = verbatim section<br/>numbers + provenance inline"]
    R2["report / topic file"]
    G["GLOSSARY.md<br/>one node per concept"]
    R1 -- "[[slug]] · Cites:" --> G
    R2 -- "Supersedes: · Kills:" --> R1
  end

  subgraph ROUTER["ROUTER — WORKLOG.md"]
    W["dated ledger, newest last<br/>status word + one-liner + pointer<br/>ALIVE / DEAD / WATCH / FROZEN"]
  end

  subgraph CACHE["CACHE — derived, disposable"]
    P["graph.py<br/>parses [[links]] + typed edges<br/>neighbors · dead · orphans"]
  end

  S(["session start"]) -- "read tail (~10 entries)" --> W
  E(["task end / pre-compact"]) -- "append entry" --> W
  W -- "pointer → ground before acting" --> TRUTH
  TRUTH -- "re-parsed every run<br/>never authoritative" --> P
  Q(["query"]) --> P
